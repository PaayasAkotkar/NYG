import { OnClickColorDirective } from './on-click-color.directive';

describe('OnClickColorDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickColorDirective();
    expect(directive).toBeTruthy();
  });
});
