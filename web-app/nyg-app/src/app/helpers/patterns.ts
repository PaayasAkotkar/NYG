export interface OCSFieldFormat {
    OCStemplate: string // to display and this is the value to be given as the formControlName
    OCSformControl: any // note this it the default value for reactive formControl
    OCSToolTip: string
}
