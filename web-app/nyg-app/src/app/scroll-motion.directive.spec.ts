import { ScrollMotion } from './scroll-motion.directive';

describe('ScrollMotionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollMotion();
    expect(directive).toBeTruthy();
  });
});
